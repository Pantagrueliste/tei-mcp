---
phase: 05-deprecation-awareness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/tei_mcp/models.py
  - src/tei_mcp/parser.py
  - tests/fixtures/test_odd.xml
  - tests/test_parser.py
autonomous: true
requirements: [DEPR-04]

must_haves:
  truths:
    - "Parser extracts @validUntil from elementSpec into ElementDef.valid_until"
    - "Parser extracts @validUntil from attDef into AttDef.valid_until"
    - "Parser extracts desc[@type='deprecationInfo'] inner XML into deprecation_info field"
    - "Non-deprecated entities have empty string valid_until and deprecation_info"
    - "Inline XML markup (e.g. <gi>entry</gi>) is preserved in deprecation_info text"
    - "Missing deprecationInfo desc produces generic fallback message"
  artifacts:
    - path: "src/tei_mcp/models.py"
      provides: "AttDef and ElementDef with valid_until and deprecation_info fields"
      contains: "valid_until: str"
    - path: "src/tei_mcp/parser.py"
      provides: "_extract_deprecation() and _inner_xml() helpers"
      contains: "_extract_deprecation"
    - path: "tests/fixtures/test_odd.xml"
      provides: "Deprecated element and attribute fixtures for testing"
      contains: "validUntil"
    - path: "tests/test_parser.py"
      provides: "Tests for deprecation field extraction"
      contains: "test_deprecated_element"
  key_links:
    - from: "src/tei_mcp/parser.py"
      to: "src/tei_mcp/models.py"
      via: "AttDef and ElementDef constructors with new fields"
      pattern: "valid_until=.*deprecation_info="
    - from: "tests/test_parser.py"
      to: "src/tei_mcp/parser.py"
      via: "parse_odd() on test fixture"
      pattern: "store\\.get_element.*valid_until"
---

<objective>
Add deprecation data extraction to the ODD parser: new model fields and parser helpers that extract @validUntil and desc[@type='deprecationInfo'] from elementSpec and attDef elements.

Purpose: DEPR-04 requires the parser to extract deprecation metadata from the ODD spec into model fields. This is the foundation that Plan 02 builds on to surface deprecation in tool responses.
Output: Updated models.py, parser.py, test fixture with deprecated entities, passing deprecation parser tests.
</objective>

<execution_context>
@/Users/cag30/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cag30/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deprecation-awareness/05-CONTEXT.md
@.planning/phases/05-deprecation-awareness/05-RESEARCH.md

<interfaces>
<!-- Current model definitions that will be extended -->

From src/tei_mcp/models.py:
```python
@dataclass(frozen=True)
class AttDef:
    ident: str
    desc: str
    datatype: str
    values: tuple[str, ...]
    closed: bool

@dataclass(frozen=True)
class ElementDef:
    ident: str
    module: str
    gloss: str
    desc: str
    classes: tuple[str, ...]
    attributes: tuple[AttDef, ...]
    content_raw: str
```

From src/tei_mcp/parser.py:
```python
NS = {"tei": "http://www.tei-c.org/ns/1.0", "rng": "http://relaxng.org/ns/structure/1.0"}
_XML_LANG = "{http://www.w3.org/XML/1998/namespace}lang"

def _text(el: ET.Element, xpath: str) -> str: ...
def _parse_att_def(el: ET.Element) -> AttDef: ...
def _parse_element_spec(el: ET.Element) -> ElementDef: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deprecated fixtures and write failing parser tests</name>
  <files>tests/fixtures/test_odd.xml, tests/test_parser.py</files>
  <behavior>
    - test_deprecated_element_fields: parse_odd on fixture -> store.get_element("re") has valid_until="2024-01-15" and deprecation_info containing "<gi>entry</gi>" markup
    - test_deprecated_element_no_deprecation_desc: parse_odd -> store.get_element("superEntry") has valid_until="2027-03-07" and deprecation_info matches generic fallback "Deprecated as of 2027-03-07. No migration guidance available."
    - test_deprecated_attdef_fields: parse_odd -> an element with a deprecated attDef has that AttDef's valid_until set and deprecation_info non-empty
    - test_nondeprecated_element_no_validuntil: parse_odd -> store.get_element("p") has valid_until=="" and deprecation_info==""
    - test_nondeprecated_attdef_no_validuntil: parse_odd -> AttDef for "part" on element "p" has valid_until=="" and deprecation_info==""
  </behavior>
  <action>
1. Add deprecated test entities to tests/fixtures/test_odd.xml:
   - A deprecated element "re" with validUntil="2024-01-15" (expired date for test determinism), a desc[@type='deprecationInfo'] containing inline XML: `Because an <gi>entry</gi> can now occur inside an <gi>entry</gi>, the <gi>re</gi> element is no longer needed.` Give it module="dictionaries", a basic desc, classes (model.common), and empty content.
   - A deprecated element "superEntry" with validUntil="2027-03-07" but NO desc[@type='deprecationInfo'] child (tests the generic fallback). Give it module="dictionaries", basic desc, empty content.
   - A classSpec "att.ref" (type="atts") with a deprecated attDef ident="name" with validUntil="2026-11-13" and a desc[@type='deprecationInfo'] containing inline XML: `Use the <att>ident</att> attribute instead.`
   - An element "attRef" (module="tagdocs") that is a memberOf "att.ref" and "att.global", with empty content.

2. Write failing tests in tests/test_parser.py (these use the existing `store` fixture from conftest.py which calls parse_odd on the test fixture):
   - test_deprecated_element_fields: Assert store.get_element("re").valid_until == "2024-01-15" and "<gi>entry</gi>" in store.get_element("re").deprecation_info
   - test_deprecated_element_fallback_info: Assert store.get_element("superEntry").valid_until == "2027-03-07" and store.get_element("superEntry").deprecation_info == "Deprecated as of 2027-03-07. No migration guidance available."
   - test_deprecated_attdef_fields: Get attRef element, find the "name" AttDef in its class's attributes (via att.ref), assert valid_until == "2026-11-13" and "ident" in deprecation_info. Actually, the attDef is on the classSpec att.ref, so get the class and check its attributes.
   - test_nondeprecated_element_no_validuntil: Assert store.get_element("p").valid_until == "" and store.get_element("p").deprecation_info == ""
   - test_nondeprecated_attdef_no_validuntil: Get element "p", find AttDef "part", assert valid_until == "" and deprecation_info == ""

3. Run tests -- they MUST FAIL (RED) because models don't have the fields yet.
  </action>
  <verify>
    <automated>cd /Users/cag30/tei-p5 && uv run pytest tests/test_parser.py -k "deprecated" -x 2>&1 | tail -5</automated>
  </verify>
  <done>5 deprecation parser tests exist and fail with AttributeError (fields don't exist yet). Test fixture has 2 deprecated elements, 1 deprecated attDef on a classSpec, and 1 element using that class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement model fields and parser extraction (GREEN)</name>
  <files>src/tei_mcp/models.py, src/tei_mcp/parser.py</files>
  <behavior>
    - All 5 tests from Task 1 pass
    - Existing test suite remains green (backward compatible)
  </behavior>
  <action>
1. In src/tei_mcp/models.py, add two fields at the END of AttDef (after `closed`):
   ```python
   valid_until: str = ""
   deprecation_info: str = ""
   ```
   Add the same two fields at the END of ElementDef (after `content_raw`):
   ```python
   valid_until: str = ""
   deprecation_info: str = ""
   ```
   Fields have defaults so all existing code (including tests) remains backward compatible.

2. In src/tei_mcp/parser.py, add a helper `_inner_xml(el: ET.Element) -> str` that extracts the inner XML content preserving inline markup. Use `el.text or ""` concatenated with `ET.tostring(child, encoding="unicode")` for each child element. Strip namespace prefixes from the output by removing `ns0:` or `{...}` patterns (use re.sub). Strip leading/trailing whitespace.

3. Add helper `_extract_deprecation(el: ET.Element) -> tuple[str, str]`:
   - Get `valid_until = el.get("validUntil", "")`
   - If valid_until is empty, return ("", "")
   - Find desc[@type='deprecationInfo'] with English preference (iterate el.findall("tei:desc", NS), check for type="deprecationInfo" and xml:lang="en" or no lang)
   - If found, use `_inner_xml(desc_el)` to get info text
   - If not found, use fallback: `f"Deprecated as of {valid_until}. No migration guidance available."`
   - Return (valid_until, info)

4. Update `_parse_att_def()`: Call `_extract_deprecation(el)` and pass results to AttDef constructor as `valid_until=` and `deprecation_info=`.

5. Update `_parse_element_spec()`: Call `_extract_deprecation(el)` and pass results to ElementDef constructor as `valid_until=` and `deprecation_info=`.

6. Update `_parse_class_spec()`: The classSpec can also have deprecated attDefs (att.ref/@name example). `_parse_att_def` already handles this since each attDef is parsed individually. No change needed to _parse_class_spec itself -- but verify the attDef deprecation flows through.

7. Add `import re` at top of parser.py if not already present (for namespace stripping in _inner_xml).

8. Run all tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/cag30/tei-p5 && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>All 5 deprecation parser tests pass. Full test suite green. AttDef and ElementDef have valid_until and deprecation_info fields with empty-string defaults. Parser extracts @validUntil and deprecationInfo from elementSpec and attDef elements, preserving inline XML markup.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_parser.py -k "deprecated" -v` -- all 5 deprecation tests pass
- `uv run pytest tests/ -x -q` -- full suite green (no regressions)
- Verify inline XML preserved: test assertion checks `<gi>entry</gi>` substring in deprecation_info
- Verify generic fallback: test assertion checks exact fallback message for superEntry
</verification>

<success_criteria>
- DEPR-04 satisfied: Parser extracts @validUntil and desc[@type='deprecationInfo'] from ODD spec into model fields
- AttDef.valid_until and AttDef.deprecation_info fields exist with empty-string defaults
- ElementDef.valid_until and ElementDef.deprecation_info fields exist with empty-string defaults
- _extract_deprecation() helper shared between _parse_att_def() and _parse_element_spec()
- Inline XML markup preserved in deprecation_info (not stripped by itertext)
- Generic fallback message used when no deprecationInfo desc exists
- All existing tests still pass (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/05-deprecation-awareness/05-01-SUMMARY.md`
</output>
